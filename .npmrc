# npm 10.9.x crashes resolving vitest 4's optional peer graph
# ("Cannot read properties of null (reading 'edgesOut')"), even for a bare
# `npm install vitest`. Relaxing peer resolution is the workaround; the
# installed tree is still the one the lockfile pins.
legacy-peer-deps=true
